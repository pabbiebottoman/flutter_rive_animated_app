import 'dart:ui';

class Recent {
  String subtitle, title, image;
  Color color;

  Recent({
    required this.title,
    required this.subtitle,
    required this.image,
    required this.color,
  });
}
